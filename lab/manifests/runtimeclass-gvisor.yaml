apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
# 'runsc' is the handler name we registered in the kind node's containerd config.
# Pods that set runtimeClassName: gvisor run under the gVisor user-space kernel.
handler: runsc
